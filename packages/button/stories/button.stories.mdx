import { Meta, Story, Canvas } from '@storybook/addon-docs'

import { Button } from '../src'

<Meta title="Button" component={Button} />

# Button

`Button` is the basic building-block of many interactive components.

export const Template = (args) => <Button {...args}>Primary</Button>

<Story
  name="primary"
  args={{
    theme: 'primary',
    onClick: () => alert('I have been clicked'),
  }}
>
  {Template.bind({})}
</Story>
